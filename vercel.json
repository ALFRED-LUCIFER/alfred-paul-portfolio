{
  "version": 2,
  "buildCommand": "pnpm install --frozen-lockfile && pnpm run build --filter=web",
  "outputDirectory": "apps/web/dist",
  "installCommand": null,
  "framework": null,
  "routes": [
    {
      "handle": "filesystem"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
